/* Placeholder until `npm run generate:types` runs. */
export type Post = {
  id: number | string
  title: string
  slug: string
  excerpt?: string | null
  bodyHtml?: string | null
  publishedAt: string
}
